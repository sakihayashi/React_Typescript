import { LOCALES } from '@otosense/locale';

export const TIMEOUT_FOR_SET_STATE: number = 25;
export const PONG: string = 'pong';
export const PING_DELAY: number = 2000;

export const availableLocales: LOCALES[] = [
    LOCALES.ENGLISH,
    LOCALES.FRENCH,
    LOCALES.HUNGARIAN,
];
