// import * as React from 'react';

// import { shallow/*, render, mount */} from 'enzyme';

// import { Recording } from '../../../src/Views/DataCollectionAndAnalysis/screens/Recording';

describe('Recording', () => {
    test('dummy test', () => expect(true).toBe(true));
    // it('wraps RecordingComponent', () => {
    //     const component: any = shallow(<Recording />);
    //     expect(component.find('RecordingComponent')).toHaveLength(1);
    // });
});
