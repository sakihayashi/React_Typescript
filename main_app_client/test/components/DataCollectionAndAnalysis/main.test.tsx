// import { shallow } from 'enzyme';
// import * as React from 'react';
// import * as RootContextModule from '../../../src/App';
// import { DataCollection } from '../../../src/Views/DataCollectionAndAnalysis/main';
// // import { Button } from '@react-md/button';
// import { testRootStore } from '../../testData';

describe('', () => {
    test('dummy test', () => expect(true).toBe(true));
    // jest.spyOn(RootContextModule, 'useRootContext').mockImplementation(() => (testRootStore));
    // const wrapper: any = shallow(
    //     <RootContextModule.RootStoreContext.Provider value={testRootStore}>
    //         <DataCollection />
    //     </RootContextModule.RootStoreContext.Provider>,
    // ).dive();

    // it('should have a class name oto-screen', () => {
    //     expect(wrapper.find('div.oto-screen')).toHaveLength(1);
    // });
    // not possible to render renderContent with shallow
    // it('should have two Button components', () => {
    //     expect(wrapper.find(Button)).toHaveLength(2);
    // });
    // it('datacollection snapshot', () => {
    //     expect(wrapper).toMatchSnapshot();
    // });
});
