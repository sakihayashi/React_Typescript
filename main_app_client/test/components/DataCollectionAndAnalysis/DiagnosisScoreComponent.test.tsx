// import { shallow } from 'enzyme';
// import React from 'react';
// import * as RootContextModule from '../../../src/App';
// import { DiagnosisScoreComponent }
// from '../../../src/Views/DataCollectionAndAnalysis/components/DiagnosisScoreComponent';
// import { testResults, testRootStore } from '../../testData';

describe('<DiagnosisScoreComponent/>', () => {
  test('dummy test', () => expect(true).toBe(true));
  // jest.spyOn(RootContextModule, 'useRootContext').mockImplementation(() => (testRootStore));
  // const wrapper: any = shallow(
  //     <RootContextModule.RootStoreContext.Provider value={testRootStore}>
  //         <DiagnosisScoreComponent results={testResults} />
  //     </RootContextModule.RootStoreContext.Provider>,
  // ).dive();
  //   // TODO test this component ..

  //   // shallow with dive does not render renderfunction
  //   // it('check if we have four elements to display for the results info', () => {
  //   //     expect(wrapper.find('div.oto-diagnosis-score__info--label')).toHaveLength(4);
  //   //   });

  // it('should have className and html div', () => {
  //       expect(wrapper.find('div.oto-diagnosis-score__diagnostics')).toHaveLength(1);
  //   });
  // it('should have expandable score', () => {
  //     expect(wrapper.find('div.oto-diagnosis-score__diagnostics-score-bar-row')).toHaveLength(1);
  //   });
});
