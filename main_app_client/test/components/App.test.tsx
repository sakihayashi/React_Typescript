// import * as React from 'react';

// import LocaleStore from '@otosense/locale';
// import { shallow, ShallowWrapper } from 'enzyme';

// import App from '../../src/App';
// import AppState from '../../src/appState';
// import Main from '../../src/Main';
// import PlaybackStore from '../../src/Utility/Playback/playbackStore';
// import { ToastStore } from '../../src/Utility/Toast';
// import { DataCollectionStore } from '../../src/Views/DataCollectionAndAnalysis';
// import ReviewSessionsStore from '../../src/Views/ReviewSessions/store';

describe('Root app', () => {
    test('dummy test', () => expect(true).toBe(true));
    // const wrapper: ShallowWrapper = shallow(<App />);
    // const component: App = wrapper.instance() as App;
    // it('should contain all necessary stores', () => {
    //     expect(component.appState).toBeInstanceOf(AppState);
    //     expect(component.dataCollectionStore).toBeInstanceOf(DataCollectionStore);
    //     expect(component.localeStore).toBeInstanceOf(LocaleStore);
    //     expect(component.playbackStore).toBeInstanceOf(PlaybackStore);
    //     expect(component.reviewSessionsStore).toBeInstanceOf(ReviewSessionsStore);
    //     expect(component.toastStore).toBeInstanceOf(ToastStore);
    // });
    // it('should render the main component', () => {
    //     expect(wrapper.contains(<Main />));
    // });
});
